<div class="row">
                 
        <div class="col-md-6">
            {{ form_row(filterForm.id) }}
            {{ form_row(filterForm.idUser) }}
        </div>
        <div class="col-md-6">
            {{ form_row(filterForm.name) }}
            {{ form_row(filterForm.info) }}
        </div>
        <div class="col-md-6">
            {{ form_row(filterForm.section) }}
        </div>
        <div class="col-md-6">
            {{ form_row(filterForm.sector) }}
            {{ form_row(filterForm.object) }}
            {{ form_row(filterForm.subobject) }}
        </div>

                    <div class="col-md-6">
        {{ form_rest(filterForm) }}
    </div>
    <div class="col-md-12">
        <div class="form-group">
            <button type="submit" class="btn btn-warning" name="filter_action" value="filter"> <span class="glyphicon glyphicon-filter" aria-hidden="true"></span> Применить фильтр</button>
            <button type="submit" class="btn btn-default" name="filter_action" value="reset"><span class="glyphicon glyphicon-minus" aria-hidden="true"></span> Сбросить</button>
        </div>
    </div>
</div>